import { BadRequestException, ConflictException, ForbiddenException, NotFoundException } from '@nestjs/common';
import { Student } from './entities/student.entity';
import { Repository } from 'typeorm';
import { User } from 'src/teacher/users/entities/user.entity';
import { Image } from 'src/teacher/images/entities/image.entity';
import { Exam } from 'src/teacher/exams/entities/exam.entity';
import { addAnswerDto, createstudDto, sendOtpDTO, UpdateStudentDtoinfo } from './dto/create-student.dto';
import { twilioService } from 'common/smsotp';
export declare class StudentsService {
    private readonly imageRepository;
    private readonly user;
    private readonly examrepo;
    private readonly repository;
    private readonly twilioservice;
    constructor(imageRepository: Repository<Image>, user: Repository<User>, examrepo: Repository<Exam>, repository: Repository<Student>, twilioservice: twilioService);
    sendSmsOtp(sendOtpDTO: sendOtpDTO): Promise<Student>;
    searchStudents(search: string): Promise<Student[]>;
    searchCourseId(search: string, id: number): Promise<Student[]>;
    mycourses(studentId: number): Promise<Student>;
    findbyId(id: number, userId: any, page: number, limit: number, attendence: boolean, exam_name: string): Promise<Student | ForbiddenException | BadRequestException>;
    addAnswer(id: number, addAnswerDto: addAnswerDto): Promise<NotFoundException | import("typeorm").UpdateResult>;
    getattendence(attendence: boolean, id: number): Promise<Student>;
    getpagination(offset: number, limit: number): Promise<Student[]>;
    getpaginationid(offset: number, limit: number, id: number): Promise<Student[]>;
    handleFileUpload(id: number, file: Express.Multer.File): Promise<any>;
    typeonline(coursetype: string): Promise<Student[]>;
    findAll(): Promise<Student[]>;
    getLastImage(students: number): Promise<Image[]>;
    search(search: string): Promise<Exam[]>;
    saveinfo(createstudDto: createstudDto): Promise<Student>;
    update(UpdateStudentDto: UpdateStudentDtoinfo, id: number): Promise<ConflictException | import("typeorm").UpdateResult>;
    profile(id: number): Promise<Student>;
}
